export enum PostStatusEnum {
  Publish = "Publish",
  Future = "Future",
  Draft = "Draft",
  Pending = "Pending",
  AutoDraft = "AutoDraft",
  Inherit = "Inherit",
}

export enum PostVisibilityEnum {
  Public = "Public",
  Private = "Private",
}

export enum PostTypeEnum {
  Post = "Post",
  Product = "Product",
  Tutorial = "Tutorial",
  Page = "Page",
}
